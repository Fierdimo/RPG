function mark_availability(dest){
	//show_debug_message("hold "+string(hold)+" >>> "+string(""))
	 if (dest == 13) colour = c_red;
	if(active_button()) active_button().holding_txt = active_button().data
	
}